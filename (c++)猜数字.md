```c++#include <iostream>using namespace std;#include <ctime>int main(){    srand((unsigned int)time(NULL));    int num = rand() % 101;    int number = 1000;    int biggest = 100;    int smallest = 1;    while (number != num)    {        cout << "请输入你要猜的数（" << smallest << " - " << biggest << "）" << endl;        cin >> number;        while (number >= biggest)        {            cout << "请输入" << smallest << " - " << biggest << "内的数字！请输入你要猜的数：" << endl;            cin >> number;        }        while (number <= smallest)        {            do            {                cout << "请输入" << smallest << " - " << biggest << "内的数字！请输入你要猜的数：" << endl;                cin >> number;            } while (number >= biggest);        }                        if (number < num)        {            cout << "数字过小" << endl;            smallest = number;        }                if (number > num)        {            cout << "数字过大" << endl;            biggest = number;        }    }    cout << "恭喜你！猜对了！" << endl;    system("pause");    return 0;}```